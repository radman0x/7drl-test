name: rl-app-skeleton

on: [push]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v1
      - name: Build
        run: |
          npm ci
          npx nx build rl-app --prod --base-href "."
